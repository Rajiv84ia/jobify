---
apiVersion: apps/v1
kind: Deployment
metadata: 
  name: backenddeployment
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      name: backendPod
      labels:  
        app: backend
    spec:
      containers:
      - name: backendcontainer
        image: rajiv84iya/jobify-backend
        ports:
        - containerPort: 8000
        resources:
          limits:
            cpu: 500m
          requests:
            cpu: 200m
...